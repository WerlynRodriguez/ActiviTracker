/**
 * A loading component to be displayed when the user is waiting for data to load.
 */
export default function () {
    return (
        <div className="hero min-h-screen">
            <span className="loading loading-ring loading-lg"></span>
        </div>
    )
}